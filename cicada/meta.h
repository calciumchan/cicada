//SDL has different include paths based on the target hardware, this file makes sure youre including the correct way
//this should be included in any file that uses SDL includes, which will probably be pretty much all of them
#include <SDL2/SDL.h>
